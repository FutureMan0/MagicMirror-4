{
  "apiVersion": 2,
  "name": "unraid",
  "displayName": "Unraid",
  "version": "1.0.0",
  "description": "Array, Platten, Temperaturen und Docker-Container des Unraid-Servers.",
  "author": "MagicMirror⁴",
  "secrets": [
    {
      "key": "apiKey",
      "env": "UNRAID_API_KEY",
      "label": "Unraid API-Schlüssel",
      "exposeToRenderer": false
    }
  ],
  "provides": [
    "data:unraid"
  ],
  "config": {
    "serverUrl": {
      "type": "string",
      "default": "",
      "description": "Adresse des Servers, z. B. https://tower.local"
    },
    "apiKey": {
      "type": "string",
      "default": "",
      "description": "Erzeugen mit: unraid-api apikey --create. Vorher unter Settings → Management Access die Developer Options einschalten."
    },
    "allowInsecureTls": {
      "type": "boolean",
      "default": true,
      "description": "Selbstsigniertes Zertifikat akzeptieren (bei Unraid der Normalfall)"
    },
    "showSystem": {
      "type": "boolean",
      "default": true,
      "description": "CPU und Arbeitsspeicher anzeigen"
    },
    "showArray": {
      "type": "boolean",
      "default": true,
      "description": "Array und Platten anzeigen"
    },
    "showDocker": {
      "type": "boolean",
      "default": true,
      "description": "Docker-Container anzeigen"
    },
    "showVms": {
      "type": "boolean",
      "default": false,
      "description": "Virtuelle Maschinen anzeigen (nicht in jeder Unraid-Fassung verfügbar)"
    },
    "maxDisks": {
      "type": "number",
      "default": 8,
      "min": 1,
      "max": 30,
      "description": "Höchstzahl angezeigter Platten"
    },
    "warnTemp": {
      "type": "number",
      "default": 45,
      "min": 30,
      "max": 70,
      "description": "Ab welcher Temperatur eine Platte auffällig dargestellt wird (°C)"
    },
    "updateInterval": {
      "type": "number",
      "default": 30000,
      "min": 10000,
      "description": "Aktualisierungsintervall in Millisekunden"
    }
  }
}
